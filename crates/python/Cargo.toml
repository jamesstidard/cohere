[package]
name = "cohere-python"
version = "0.1.0"
edition = "2021"

[lib]
name = "cohere"
crate-type = ["cdylib"]

[dependencies]
cohere-core = { path = "../core" }
pyo3 = { version = "0.20", features = ["extension-module"] }
serde_json = "1"
