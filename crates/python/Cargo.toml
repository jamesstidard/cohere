[package]
name = "graph-validator-python"
version = "0.1.0"
edition = "2021"

[lib]
name = "graph_validator"
crate-type = ["cdylib"]

[dependencies]
graph-validator-core = { path = "../core" }
pyo3 = { version = "0.20", features = ["extension-module"] }
serde_json = "1"
